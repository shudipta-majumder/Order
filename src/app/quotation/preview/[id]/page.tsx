import QuotationPreview from "@/components/pageComponents/quotation/preview/QuotationPreview";

type Props = {
  params: {};
};

const Preview = ({ params }: Props) => {
  return <QuotationPreview params={params} />;
};

export default Preview;
