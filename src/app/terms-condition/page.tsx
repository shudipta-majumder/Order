"use client";
import TermsConditionPolicy from "@/components/pageComponents/quotation/view/TermsConditionPolicy";

type Props = {
  params: { ref: string };
};

export default function SimplePaper() {
  return <TermsConditionPolicy />;
}
